services:
  - type: web
    name: hello-dalat-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: RENDER_URL
        sync: false
      - key: FIREBASE_DB_URL
        sync: false
      - key: FIREBASE_CRED_PATH
        sync: false
